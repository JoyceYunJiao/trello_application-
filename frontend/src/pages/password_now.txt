import {useState} from 'react';
import * as React from 'react';
import Avatar from '@mui/material/Avatar';
import Button from '@mui/material/Button';
import CssBaseline from '@mui/material/CssBaseline';
import TextField from '@mui/material/TextField';
import FormControlLabel from '@mui/material/FormControlLabel';
import Checkbox from '@mui/material/Checkbox';
import Link from '@mui/material/Link';
import Grid from '@mui/material/Grid';
import Box from '@mui/material/Box';
import LockOutlinedIcon from '@mui/icons-material/LockOutlined';
import Typography from '@mui/material/Typography';
import Container from '@mui/material/Container';
import { createTheme, ThemeProvider } from '@mui/material/styles';
import { Redirect } from 'react-router-dom';


const theme = createTheme();

export default function ForgotPassword() {

  const [SecAnsStatus, setSecAnsStatus] = useState(false);
  const [succeed, setSucceed] = useState(false);

  const pageRedirect = ()=> {
    // window.location.replace("http://127.0.0.1:3000/Login");
  }      

  const handleSubmit = (event) => {
    event.preventDefault();
    //collect everything in data
    const data = new FormData(event.currentTarget);
    console.log(data);
    fetch('http://localhost:9001/showUserByPassword/'+data.get("email"))
    .then(response => {
        //this email address is correct
        console.log("position1");
        console.log(data.get("email"));
        // const parsedResponse = JSON.parse(response);
        if(response.ok){
            // console.log(parsedResponse);
            console.log("position2");

            setSecAnsStatus(response.securityAnswer===data.get("securityAnswer"));
            console.log("position2 setSecAnsStatus: "+SecAnsStatus+", response2 is "+response.securityAnswer);
            console.log("position2.5");
            console.log(response.message)



            //when input the correct email address and the sectury answer is yes
            // SecAnsStatus
            if(SecAnsStatus){
                console.log("position3");

                //change password in database
                fetch('http://localhost:9001/updateUserPasswordByEmail?email='+data.get("email")+'&newPassword='+data.get("password"), {
                    method: 'POST',
                    headers: {
                    'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({
                    emailId: data.get("email"),
                    password: data.get("password")
                    })
                })
                setSucceed(true);
                // return response.json();
            }

            console.log({
                emailId: data.get("email"),
                password: data.get("password"),
                securityAnswer: data.get("securityAnswer")
            });
        }
        else {
            //Security Answer Status is no
            if(!SecAnsStatus){
                console.log("Security Answer Status is no , please answer firstly");
            }
            if(response==null){
                console.log("wrong mail address and return: "+ response.message );
                return Promise.reject('The user does not exists');
            }
        }

    })
    // .then(data => {
    //     console.log(data)
    //     // console.log(data.securityAnswer)
    // })
    .catch(error => {
        console.error(error);
    console.log('The user can be added: ');
    console.log({
        emailId: data.get("email"),
        password: data.get("password"),
        securityAnswer: data.get("securityAnswer")

    });

        //Redirect to another page
        setTimeout(pageRedirect, 3000);
      });

  };

  return (
    <ThemeProvider theme={theme}>
      <Container component="main" maxWidth="xs">
        <CssBaseline />
        <Box
          sx={{
            marginTop: 8,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
          }}
        >
          <Avatar sx={{ m: 1, bgcolor: 'secondary.main' }}>
            <LockOutlinedIcon />
          </Avatar>
          <Typography component="h1" variant="h5">
            Forgot Password 
          </Typography>
          <Box component="form" noValidate onSubmit={handleSubmit} sx={{ mt: 3 }}>
            <Grid container spacing={2}>
              <Grid item xs={12}>
                <TextField
                  required
                  fullWidth
                  id="email"
                  label={succeed?"This email address is not existed":"Email Address"}
                  name="email"
                  autoComplete="email"
                  error={succeed}
                />
              </Grid>

              <Grid item xs={12}>
                <TextField
                  required
                  fullWidth
                  name="securityAnswer"
                  label="Security Answer"
                  type="securityAnswer"
                  id="securityAnswer"
                  autoComplete="securityAnswer"
                />
              </Grid>

              <Grid item xs={12}>
                <TextField
                  required
                  fullWidth
                  name="password"
                  label="New password"
                  type="password"
                  id="password"
                  autoComplete="new-password"
                />
              </Grid>

            </Grid>
            <Button
              type="submit"
              fullWidth
              variant="contained"
              color={succeed?"success":"primary"}
              sx={{ mt: 3, mb: 2 }}
            >
              {succeed? "Reset success": "Reset password"}
            </Button>
            <Grid container justifyContent="flex-end">
              <Grid item>
                <Link href="/Login" variant="body2">
                  Rember your password again? This way sir→
                </Link>
              </Grid>
            </Grid>
          </Box>
        </Box>
        {/* <Copyright sx={{ mt: 5 }} /> */}
      </Container>
    </ThemeProvider>
  );
}